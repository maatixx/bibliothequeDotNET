namespace LibraryManager.App
{
    public class Book
    {
        // Initialize to empty strings to satisfy nullable reference types
        public string Name { get; set; } = string.Empty;
        public string Type { get; set; } = string.Empty;
    }
}
